# 2026-08-03

## GitHub Pages 部署配置

### 背景
用户询问如何部署 Quartz 到 GitHub Pages。现有配置：
- 仓库：lawsums/Quartz_base1，分支 v5
- 原有 workflow（deploy-v5.yaml, deploy-preview.yaml）是上游 jackyzha0/quartz 的 Cloudflare Pages 部署，有仓库条件检查，不会在 fork 中运行
- baseUrl 原为 `lawsam.github.io`（与实际 GitHub 用户名 lawsums 不一致）

### 完成的工作

1. **修正 baseUrl**：`lawsam.github.io` → `lawsums.github.io/Quartz_base1`
   - 项目站点（非用户站点），URL 格式为 `https://lawsums.github.io/Quartz_base1/`

2. **创建 GitHub Actions 部署 workflow**：`.github/workflows/deploy.yml`
   - 触发条件：push v5 分支
   - 步骤：checkout → Node 22 → npm ci → npm run build → configure-pages → upload-pages-artifact → deploy-pages
   - 使用 GitHub 官方的 deploy-pages action，不需要 gh-pages 分支

3. **创建 robots.txt**：`quartz/static/robots.txt`
   - Allow: /，Sitemap 指向 lawsums.github.io/Quartz_base1/sitemap.xml

4. **修改 .gitignore**：`scripts/` 排除 → `scripts/rawg-config.json` 排除
   - prebuild 中的 generate-projects / cache-covers / generate-anime-pages 需要 scripts/ 中的脚本
   - rawg-config.json 含 API key，仍被排除；*.log 全局排除保留

5. **提交** 11 files（已 commit，待 push）

### 推送被阻止
- Sandbox 阻止了 SSH key 访问（~/.ssh/known_hosts, id_rsa 等）
- 用户拒绝了沙箱权限，需手动推送

### 后续待办
- 仓库中还有一个未追踪的新文件：`quartz/static/project-query-inline.js`
- 多个文件有未提交的修改（番剧库.md, package.json, Head.tsx, project-query.js, project-styles.css）
- 部分 Leetcode 内容文件被标记删除（1.md~5.md），可能是 sync-obsidian 脚本的 /MIR 同步结果
