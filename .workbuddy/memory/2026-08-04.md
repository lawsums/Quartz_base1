# 2026-08-04

## GitHub Actions 构建失败排查与修复
- **问题 1**：CI 构建 `npm run build` 失败，Annotations 显示 1 error (exit code 1)
- **根因**：`install-plugins` 使用 `npx tsx ./quartz/plugins/loader/install-plugins.ts`，导入链 quartz.ts → config-loader → componentResources.ts → `import baseStyles from "../../styles/base.scss"`，tsx 无法处理 .scss 文件，抛出 `ERR_UNKNOWN_FILE_EXTENSION`
- **修复**：将 install-plugins 改为 `node ./quartz/bootstrap-cli.mjs plugin install`（quartz CLI 用 esbuild 内部处理所有文件类型）
- **问题 2**：Node 20 弃用警告（actions/checkout@v4 和 setup-node@v4 内部使用 Node 20）
  - 此为无害警告，无 v5 版本可升级，不影响构建
- 本地 prebuild 已验证通过（4步全部成功）
- commit: `3dab0fc` fix: 修复 CI 构建中 install-plugins 的 .scss 导入错误

## GitHub Pages 部署成功
- 2026-08-05T09:36:38Z Deploy to GitHub Pages workflow **成功** ✓
- 网站已上线：https://lawsums.github.io/Quartz_base1/
- 前两次构建失败（c62bdc9 / df731af），第三次成功（3dab0fc）
- 当前首页显示默认 Quartz 欢迎页（content/index.md），内容页面通过侧边栏 Explorer 访问

## 番剧库卡片不渲染修复
- **问题**：GitHub Pages 上番剧库页面卡片不显示（本地正常），页面只有"在看/想看/已看"标题
- **排查**：内嵌 JSON 数据 + 封面图 + project-styles.css + project-query.js 均正常部署
- **根因**：Head.tsx 只引用了 project-query.js（fetch 模式），漏了 project-query-inline.js（内嵌 JSON 模式）
- **修复**：Head.tsx 添加 `<script src="/static/project-query-inline.js" defer></script>`
- commit: `253e061`
- 已推送成功

## 项目站点路径 bug（绝对路径 → 404）
- **问题**：番剧库卡片仍不显示，对比本地预览和 GitHub Pages 确认是资源加载问题
- **根因**：网站 URL `lawsums.github.io/Quartz_base1/`（项目站点格式），但所有资源引用使用绝对路径 `/static/...`
  - 浏览器解析 `/static/xxx` → `https://lawsums.github.io/static/xxx` ❌ 404
  - 正确路径应为 `https://lawsums.github.io/Quartz_base1/static/xxx`
- **修复**（commit: `7dd79d2`）：所有 `/static/` 改为相对路径 `./static/`，同时适配本地预览和项目站点
  1. Head.tsx: 4 处（project-styles.css, project-query.js, project-query-inline.js, projects-index.json）
  2. project-query.js: JSON_URL
  3. cache-covers.js: webPath + 3 处 startsWith 检测逻辑
  4. 番剧库.md: link href + script src + JSON 内 cover 路径 + 恢复 PLACEHOLDER_JSON 机制
- 本地构建验证通过，所有路径正确
